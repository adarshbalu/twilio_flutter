name: twilio_flutter
description: A Package that helps with twilio API services. Features include sending SMS and WhatsApp messages among others.
version: 0.4.1
homepage: https://github.com/adarshbalu/twilio_flutter

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  http: ^1.2.0
  get_it: ^7.6.7
  logging: ^1.0.0

dev_dependencies:
  lints: ^4.0.0
